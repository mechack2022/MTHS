server:
  port: 8080

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/mthsdb
    username: postgres
    password: mechack200
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  # Optional: Logging level
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
